# UI

HUD and menu UI components live here. `Hud` is the in-game overlay showing HP,
gas, outer/inner ring integrity, wave, score, and remaining citizens, plus the
weak-point cue. `Menu` and `UiText` provide the shared pixel-art UI helpers used
by the title, settings, and game-over screens.
