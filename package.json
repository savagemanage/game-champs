{
  "name": "game-lastwar",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "LAST SQUAD (라스트 스쿼드) - an original Last War-style lane gate-runner browser game: an auto-running squad, math gates that grow/shrink the crowd, auto-fire on enemy clusters, a boss showdown, and meta-upgrades between runs, built with Phaser 3.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  }
}
