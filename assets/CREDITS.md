# Asset Credits

Third-party assets bundled with wirework. Every entry is CC0 / public-domain /
otherwise redistribution-safe. Procedurally-generated assets are the project's
own work and are listed for provenance but need no external license.

This file is append-only: later features (e.g. textures/visuals) add more
entries under the matching section below. Do not renumber existing entries.

## Audio (FEAT-002)

### Downloaded (license-verified)

- **File:** `assets/audio/ui_click.ogg`
  **Source URL:** https://kenney.nl/assets/interface-sounds (pack: "Interface Sounds" 1.0, file `Audio/click_001.ogg`)
  **Author:** Kenney (www.kenney.nl)
  **License:** CC0 1.0 (public domain) — https://creativecommons.org/publicdomain/zero/1.0/
  **Notes:** Real OGG Vorbis (mono, 44100 Hz), verified with `file` (Ogg/Vorbis, ~4.8 KB, not an HTML error page). Used for the UI click sfx (`SfxBank.UI_CLICK`). Its sibling `.import` is NOT committed because this repo's `.gitignore` excludes `*.import` (same policy as the generated `ui.*.translation` files); a one-time editor open regenerates `assets/audio/ui_click.ogg.import` + `.godot/imported/…oggvorbisstr`. Until then the OGG will not load in a headless run — the ten procedural WAV `.tres` clips below need no import and always work, so the game is never silent by construction.

### Procedurally generated (project's own, no external license)

Generated by `assets/audio/_gen_sfx.py` as committed 16-bit PCM
`AudioStreamWAV` `.tres` resources (22050 Hz, mono). These need no editor
`.import` step and load immediately. Regenerate by re-running the script.

| File | SFX | Synthesis |
|---|---|---|
| `sfx_grapple_fire.tres` | grapple fire | downward pitch chirp |
| `sfx_grapple_attach.tres` | grapple attach | two-tone metallic clink |
| `sfx_swing_whoosh.tres` | swing whoosh | band-shaped noise swell |
| `sfx_slash_swing.tres` | slash swing | sharp airy noise sweep |
| `sfx_slash_sub.tres` | slash sub-threshold hit | dull damped metallic "tunk" |
| `sfx_slash_kill.tres` | slash kill | bright ascending triad chime |
| `sfx_titan_footstep.tres` | titan footstep | low sine thud + noise transient |
| `sfx_titan_aggro.tres` | titan aggro | rising ominous tone |
| `sfx_titan_death.tres` | titan death | descending low boom + rumble |
| `sfx_player_jump.tres` | player jump | quick upward blip |
| `sfx_player_land.tres` | player land | soft low thud |

## Textures & Particles (FEAT-003)

### Downloaded (license-verified, CC0 / public domain)

All texture/particle files below are real binaries verified with `file`
(`JPEG image data, 1024x1024` / `PNG image data, 512x512`) and a sane byte size
(hundreds of KB to ~2.5 MB each, not a 7–33 KB HTML error page). They are the
specific maps actually referenced by the scenes; every other file in each source
pack (AO, displacement, metalness, DirectX-convention normals, and the other
particle sprites) was deleted to keep the repo lean.

| File | Source URL | Author | License | Notes |
|---|---|---|---|---|
| `assets/textures/ground_color.jpg` | https://ambientcg.com/a/Ground110 (`Ground110_1K-JPG.zip` → `Ground110_1K-JPG_Color.jpg`) | ambientCG (Lennart Demes) | CC0 1.0 — https://creativecommons.org/publicdomain/zero/1.0/ | Arena base ground albedo; `Mat_ground`, `uv1_scale = (16,16,1)`. |
| `assets/textures/ground_normal.jpg` | https://ambientcg.com/a/Ground110 (`Ground110_1K-JPG_NormalGL.jpg`) | ambientCG | CC0 1.0 | OpenGL-convention normal (Godot expects GL, not DX). `Mat_ground` normal_texture. |
| `assets/textures/ground_roughness.jpg` | https://ambientcg.com/a/Ground110 (`Ground110_1K-JPG_Roughness.jpg`) | ambientCG | CC0 1.0 | `Mat_ground` roughness_texture. |
| `assets/textures/rock_color.jpg` | https://ambientcg.com/a/Rock051 (`Rock051_1K-JPG_Color.jpg`) | ambientCG | CC0 1.0 | Plateaus/towers albedo; `Mat_rock`, `uv1_scale = (4,4,1)`. |
| `assets/textures/rock_normal.jpg` | https://ambientcg.com/a/Rock051 (`Rock051_1K-JPG_NormalGL.jpg`) | ambientCG | CC0 1.0 | `Mat_rock` normal_texture. |
| `assets/textures/rock_roughness.jpg` | https://ambientcg.com/a/Rock051 (`Rock051_1K-JPG_Roughness.jpg`) | ambientCG | CC0 1.0 | `Mat_rock` roughness_texture. |
| `assets/textures/ridge_color.jpg` | https://ambientcg.com/a/Rock035 (`Rock035_1K-JPG_Color.jpg`) | ambientCG | CC0 1.0 | Ramps/ridge albedo; `Mat_ridge`, `uv1_scale = (4,4,1)`. |
| `assets/textures/ridge_normal.jpg` | https://ambientcg.com/a/Rock035 (`Rock035_1K-JPG_NormalGL.jpg`) | ambientCG | CC0 1.0 | `Mat_ridge` normal_texture. |
| `assets/textures/ridge_roughness.jpg` | https://ambientcg.com/a/Rock035 (`Rock035_1K-JPG_Roughness.jpg`) | ambientCG | CC0 1.0 | `Mat_ridge` roughness_texture. |
| `assets/particles/spark.png` | https://kenney.nl/assets/particle-pack (pack "Particle Pack" 1.0, `PNG (Transparent)/spark_04.png`) | Kenney (www.kenney.nl) | CC0 1.0 — https://creativecommons.org/publicdomain/zero/1.0/ | Additive billboard albedo on the FEAT-001 slash-hit CPUParticles3D burst (`scripts/player/slash_fx.gd`). |

ambientCG confirms every asset is released under CC0 1.0 / Public Domain (see
the "Public Domain (CC0)" statement on each asset page linked above). Kenney's
Particle Pack is likewise CC0 (per kenney.nl/assets/particle-pack).

### One-time editor import (REQUIRED — read this)

This repo's `.gitignore` excludes `*.import` and `.godot/` (the same policy that
keeps the generated `ui.*.translation` files out of git), so the committed
source images ship **without** their `.import` sidecars. Godot generates those
on first editor open. Therefore:

1. Open the project once in the Godot 4.7.2 editor (or run any headless import
   pass). Godot creates `assets/**/*.import` and the imported textures under
   `.godot/imported/`, and the `res://assets/...` paths referenced by
   `scenes/Arena.tscn` and `scripts/player/slash_fx.gd` resolve.
2. Until that first import, Godot shows a placeholder for a not-yet-imported
   texture rather than crashing — the scenes still parse as valid `format=3`
   text. The slash spark loads through `ResourceLoader.exists()` guarding
   `load()`, so an unimported/headless run silently falls back to the FEAT-001
   procedural additive quad and never errors.

We deliberately did NOT hand-author `.import` files: they would be immediately
re-ignored by `.gitignore` and could drift from what the editor generates.
