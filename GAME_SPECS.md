# Open Games 제품 스펙 인덱스

이 문서는 저장소에 포함된 플레이 가능한 게임의 목표 제품 스펙을 찾기 위한 인덱스다. `_template`은 스캐폴드이고 `shared`는 공통 라이브러리이므로 게임 스펙 대상에서 제외한다.

## 문서 사용 원칙

- 각 `packages/<game>/SPEC.md`는 해당 게임이 **무엇이어야 하는지**를 정의하는 권위 있는 목표 제품 계약이다.
- 구현 현황을 설명하는 `README.md`, 작업 메모인 `handoff.md`, 현재 코드·테스트와 스펙이 충돌하면 제품 목표는 `SPEC.md`를 우선한다.
- 실제 구현이 스펙을 만족하지 않는 항목은 스펙을 낮추거나 모호하게 바꾸지 않고 각 문서의 **구현 현황 및 갭**에서 추적한다.
- 밸런스 수치 변경은 `SPEC.md`, `src/config`, 순수 규칙 테스트를 같은 변경에서 동기화해야 한다.
- 모든 게임은 정적 배포, 로컬 우선, 오리지널 IP, 무결제, 사용자에게 정직한 싱글플레이 표현을 기본으로 한다.
- `MUST`는 출시 차단 요구, `MUST NOT`은 금지, `SHOULD`는 근거 있는 예외만 허용하는 강한 요구, `MAY`는 선택 요구를 뜻한다.

## 게임별 스펙

| 게임 | 장르와 핵심 약속 | 목표 세션 | 스펙 |
|---|---|---:|---|
| Arena Champions / 아레나 챔피언스 | AI 9명과 즐기는 5v5 로컬 MOBA. 2개 전장, 10챔피언, 15분 확정 종료 | 10~15분 | [`packages/champs/SPEC.md`](packages/champs/SPEC.md) |
| Kingdom Rise / 킹덤 라이즈 | 생산·화롯불·건설·훈련·연구를 조합해 수동으로 20회 침략을 막는 전략/방치 게임 | 3~15분씩 누적 | [`packages/kingshot/SPEC.md`](packages/kingshot/SPEC.md) |
| LAST SQUAD / 라스트 스쿼드 | 기지 성장, 15영웅 2+3 편성, 결정론적 오토배틀, 2레인 Falcon Rescue가 순환하는 세로형 수집 전략 | 5~15분 | [`packages/lastwar/SPEC.md`](packages/lastwar/SPEC.md) |
| Frosthold: Last Ember / 서리성채: 마지막 불씨 | wood·coal을 태워 Warmth를 유지하고 생존자·영웅·군대를 성장시키는 혹한 생존 도시 전략 | 5~15분씩 누적 | [`packages/whiteout/SPEC.md`](packages/whiteout/SPEC.md) |
| Wirework | 축전 테더·플링·후방 냉각 노드 절단으로 중심구 주민과 이중 방호환을 8웨이브 동안 지키는 데스크톱 우선 테크 판타지 액션 | 3~15분 | [`packages/wirework/SPEC.md`](packages/wirework/SPEC.md) |

## 공통 출시 품질 계약

게임별 세부 수치와 예외는 각 스펙이 우선하며, 아래 항목은 모든 게임에 공통으로 적용한다.

1. **완결성:** 첫 실행부터 핵심 루프의 승리 또는 패배까지 계정·서버·결제 없이 완료할 수 있어야 한다.
2. **정직성:** NPC·AI·로컬 시뮬레이션을 실제 사람, 온라인 PvP, 서버 길드처럼 오인시키지 않아야 한다.
3. **결정론:** 전투·보상·오프라인 정산처럼 결과에 영향을 주는 규칙은 가능한 한 순수 함수와 명시적 seed/time 입력으로 재현 가능해야 한다.
4. **수치 단일 원본:** 경제·전투·웨이브·저장 버전의 실행 수치는 중앙 config에 두고 UI와 테스트에서 복제하지 않아야 한다.
5. **저장 안전:** localStorage 실패·손상·구버전 입력이 앱을 중단시키면 안 되며, 진행 손실 가능성을 사용자에게 숨기지 않아야 한다.
6. **입력과 접근성:** 각 게임이 공식 지원하는 입력 장치만으로 핵심 흐름을 완료할 수 있어야 하고, 메뉴·설정·결과는 키보드와 보조기술이 읽을 수 있어야 한다.
7. **반응형:** 논리 좌표를 왜곡하지 않고 지원 viewport, DPR, safe area, 회전·resize 뒤에도 입력과 HUD가 일치해야 한다.
8. **현지화:** 한국어와 영어의 기능·정보가 동등해야 하며 raw key, 잘린 핵심 문구, 언어 변경 후 혼합 언어를 허용하지 않는다.
9. **시청각 대체:** 필수 상태를 색이나 소리 하나에만 의존하지 않고 텍스트·형태·아이콘·모션 중 다른 채널로 중복 표현해야 한다.
10. **성능과 수명주기:** 장시간 플레이·씬 전환·재시도 뒤 listener, timer, tween, audio, texture가 누적되지 않아야 한다.
11. **프라이버시:** 원격 분석, 광고 SDK, 사용자 식별, 계정, 외부 업로드는 각 스펙에서 명시적으로 승인하지 않는 한 금지한다.
12. **검증:** 저장소 기여 규칙의 typecheck, test, build, docs 검사를 통과하고, 게임별 스펙의 정량 수용 기준을 별도로 검증해야 한다.

## 변경 절차

1. 제품 동작을 바꾸기 전에 해당 `SPEC.md`의 요구·수치·수용 기준을 수정한다.
2. 실행 가능한 수치를 `src/config` 또는 해당 순수 데이터 모듈에 반영한다.
3. 규칙 구현과 UI 설명을 함께 변경한다.
4. 기존 테스트를 갱신하고 변경된 규칙의 경계·실패·중복 지급·저장 복구 사례를 검증한다.
5. `구현 현황 및 갭` 표에서 해결한 항목을 갱신한다.
6. 사용자에게 노출되는 사용법이 바뀌면 해당 게임 `README.md`도 함께 갱신한다.
